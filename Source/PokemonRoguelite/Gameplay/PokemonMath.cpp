#include "PokemonMath.h"

int PokemonMath::CalculateDamage(UAttackData* Attack, ABattlePokemon* Attacker, ABattlePokemon* Defender)
{
    int Level = Attacker->GetLevel();
    int A = 0;
    // if(Attack->Category)
    
    return 0;
}
